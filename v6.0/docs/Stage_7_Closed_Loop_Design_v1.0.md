一、先明确方案1的边界现在完成的是：Stage 7A — Closed-Loop Straight Driving DesignStage 7闭环直行设计阶段，软件实现和实体调参尚未开始。当前不要：修改Atlas_6_0_Stage_6C_V4.ino；使用AA电池运行电机；调整左右PWM；猜测Kp；编写完整PID；把Stage 7记录为正式完成。计划书规定的正确顺序是：先记录同PWM下的速度差，再做比例控制，最后才考虑PID。正式测试需要50cm、1m、2m，每个距离至少5次。二、把当前硬件基线写进设计书在文件中建立以下表格：项目	当前锁定值控制器	ESP32 DEVKIT_C驱动器	L298N左编码器	GPIO25/26右编码器	GPIO18/19左电机PWM	GPIO13右电机PWM	GPIO33轮子周长	21.40 cm左轮PPR	632.05右轮PPR	634.17基准PWM	左右均为160PWM允许范围	80–220堵转时间	700 ms目标运动总超时	12 s编码器方式	A相上升沿，1X计数这些都是V4当前已经锁定的数据。特别注明：Stage 7控制器只能使用已经经过方向转换的physicalLeft和physicalRight，不能直接用right raw判断方向。右轮原始计数为负并不是错误。三、定义Stage 7的控制目标在设计书中填写：Atlas直行时，左右轮在相同时间内应移动接近相同的实际距离。控制器每隔固定时间读取左右编码器，根据左右轮速度差微调PWM，使速度差逐渐缩小。Stage 7第一阶段的目标不是精确控制绝对速度，而是：左轮实际速度 ≈ 右轮实际速度例如，不要求两边一定都是20 cm/s；首先要求两边尽可能相同。四、把Stage 7拆成三个子阶段这是成功率最高的顺序。Stage 7A：同PWM基准测试新电源到货并通过Stage 6C复验后：左右轮都使用PWM 160。不进行实时PWM修正。记录左右轮单位时间速度。至少完成5次。判断哪一侧长期更快。同时判断数据是否具有重复性。这一步只回答：在相同PWM下，左轮和右轮真实速度相差多少？Stage 7B：固定PWM补偿如果5次结果都显示左轮较快：右轮PWM保持160；左轮PWM先减少5；重新完成5次测试；比较速度差有没有缩小。例如：左轮PWM：155右轮PWM：160如果右轮较快，就反过来只调整右轮。这一阶段严格遵守“一次只改变一个变量”，不要同时改变两边PWM。Stage 7C：比例控制固定补偿稳定后，才增加实时比例控制：速度误差 → PWM修正暂时不加入积分I和微分D。五、规定采样方法设计阶段先使用：CONTROL_INTERVAL_MS = 100 ms这是Stage 7的初始设计值，不写入V4。之后通过软件模拟和新电源实测确认。每隔100ms保存一次：当前左轮计数当前右轮计数上一次左轮计数上一次右轮计数计算本周期新增脉冲：ΔL=Lcurrent	​−Lprevious	​ΔR=Rcurrent	​−Rprevious	​不要比较从启动开始的总脉冲，因为闭环需要知道“最近100ms内”的速度。六、把脉冲转换成实际速度因为左右轮PPR略有不同，不能只比较原始脉冲数。使用：LeftSpeed=632.05ΔL	​×0.121.4	​RightSpeed=634.17ΔR	​×0.121.4	​结果单位是：cm/s手算示例假设100ms内：左轮新增32个脉冲右轮新增28个脉冲左轮速度：32÷632.05×21.4÷0.1≈10.84 cm/s右轮速度：28÷634.17×21.4÷0.1≈9.45 cm/s因此左轮比右轮快约：10.84−9.45=1.39 cm/s七、固定误差的正负定义必须只使用一种定义：error=LeftSpeed−RightSpeed误差	意义	修正方向error > 0	左轮更快	降低左PWM，提高右PWMerror < 0	右轮更快	提高左PWM，降低右PWMerror ≈ 0	两轮接近	保持当前PWM把这一条写进设计书，之后不能临时反过来。八、定义比例控制公式比例控制：correction=Kp	​×errorPWM输出：LeftPWM=160−correctionRightPWM=160+correction这可以让平均PWM大致维持在160附近。教学示例假设：error = +1.39 cm/sKp = 2则：correction=2×1.39≈3输出变为：Left PWM  = 160 - 3 = 157Right PWM = 160 + 3 = 163这只是用来确认计算方向的教学示例，Kp=2不是实体测试的最终参数。最终Kp必须等新电源稳定、Stage 7A基准数据完成后再确定。九、加入三个必要的稳定限制先把以下规则写入设计书，之后实现代码时使用。1. 最大修正量初始限制：MAX_CORRECTION_PWM = 20无论误差多大，修正范围暂时不超过：左PWM：140–180右PWM：140–180这避免一次错误读数让PWM突然大幅变化。2. 每周期最大变化量MAX_PWM_STEP = 5每100ms，单侧PWM最多变化5，不能从140突然跳到180。3. 误差死区设计中先写：DEADBAND_PERCENT = 等待Stage 7A数据确定不要现在猜最终数值。死区的作用是：当左右速度已经非常接近时，不再反复加减PWM。十、保留V4的全部安全规则Stage 7不能删除或绕过V4中的：上电默认停止；每次重启后必须重新执行：armcheck leftarmcheck right两个编码器检查未通过，闭环运动必须被锁定；每次运动前必须arm；stop拥有最高优先级；任一轮700ms没有有效进展，立即停止；出现异常脉冲，立即停止；目标运动超过12秒，立即停止；左右轮分别到达目标后分别停止；停止后自动解除武装。V4已经具备方向学习、ARM门禁、独立目标停车、堵转和异常脉冲保护，Stage 7应该在它上面增加速度调节，而不是重写这些安全逻辑。十一、确定未来的数据记录格式在设计书中加入：字段	含义time_ms	当前时间left_count	左轮累计前进脉冲right_count	右轮累计前进脉冲left_delta	本周期左轮新增脉冲right_delta	本周期右轮新增脉冲left_speed_cm_s	左轮实际速度right_speed_cm_s	右轮实际速度error_cm_s	左速度减右速度correction_pwm	本周期修正量left_pwm	左轮最终PWMright_pwm	右轮最终PWMreason_code	停止或错误原因这一部分会直接连接之后的方案3——CSV数据记录工具。十二、方案1的完成验收满足以下所有条件，就可以记录“方案1完成”：已建立Stage_7_Closed_Loop_Design_v1.0.md；已写入当前21.4cm、左右PPR和PWM160；已明确Stage 7A、7B、7C三个顺序；已写出左右速度计算公式；已固定error = leftSpeed - rightSpeed；已写出PWM修正方向；已写出比例控制公式；已说明Kp为什么暂时不能确定；已列出最大修正、渐变限制和待确定死区；已保留V4的全部安全机制；没有修改V4代码；没有运行电机；Eric能够独立回答以下四个问题：为什么不能直接比较左右raw的正负？为什么速度要使用“本周期新增脉冲”计算？error > 0时是哪一个轮子更快？为什么现在不能确定最终Kp？工程日志中的状态应填写为：Stage 7 closed-loop design completed.Software implementation not started.Physical calibration pending stable bench power supply.Stage 7 is not yet formally completed.完成以上内容后，方案1就真正结束。下一步应进入方案2：用模拟左右轮数据验证这些公式和修正方向，仍然不需要运行实体电机。