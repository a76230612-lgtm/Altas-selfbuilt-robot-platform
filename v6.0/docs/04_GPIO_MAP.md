# 04 — Atlas 6.0 GPIO总表状态说明：`LOCKED`表示已确认且禁止擅改；`TBD`表示待确认且禁止接线。| 功能 | GPIO | 状态 | 是否允许修改 | 备注 ||---|---:|---|---|---|| Pan水平舵机 | 18 | LOCKED | 禁止擅改 | Atlas 5.0已确认 || Tilt垂直舵机 | 19 | LOCKED | 禁止擅改 | Atlas 5.0已确认 || 绿色LED | TBD | 待确认 | 未确认前禁止接线 | 读取5.0最终固件 || 黄色LED | TBD | 待确认 | 未确认前禁止接线 | 读取5.0最终固件 || 红色LED | TBD | 待确认 | 未确认前禁止接线 | 读取5.0最终固件 || OLED SDA | TBD | 待确认 | 未确认前禁止接线 | 检查I2C默认脚与5.0代码 || OLED SCL | TBD | 待确认 | 未确认前禁止接线 | 检查I2C默认脚与5.0代码 || 左电机PWM | TBD | 6.0新增 | 确认后锁定 | 需支持PWM || 左电机方向1 | TBD | 6.0新增 | 确认后锁定 | 检查冲突 || 左电机方向2 | TBD | 6.0新增 | 确认后锁定 | 检查冲突 || 右电机PWM | TBD | 6.0新增 | 确认后锁定 | 需支持PWM || 右电机方向1 | TBD | 6.0新增 | 确认后锁定 | 检查冲突 || 右电机方向2 | TBD | 6.0新增 | 确认后锁定 | 检查冲突 || TB6612 STBY | TBD | 6.0新增 | 确认后锁定 | 上电默认禁用 || 左编码器A/B | TBD | 6.0新增 | 确认后锁定 | 检查中断能力与输入电平 || 右编码器A/B | TBD | 6.0新增 | 确认后锁定 | 检查中断能力与输入电平 || US-100 Trigger/RX | TBD | 6.0新增 | 确认后锁定 | 先确认工作模式 || US-100 Echo/TX | TBD | 6.0新增 | 确认后锁定 | 检查3.3V逻辑安全 || 急停输入 | TBD | 6.0新增 | 确认后锁定 | 设计后再分配 |## 分配前检查1. 确认ESP32准确型号和可用GPIO。2. 检查启动绑带脚、输入专用脚、Flash/PSRAM占用和串口占用。3. 检查GPIO 18、19及所有5.0已用引脚冲突。4. 检查编码器输入电压不能超过ESP32允许范围。5. Eric确认完整GPIO表后才能接线和写运动固件。| 左电机动力线 | L298N  || ------ | ------ || `M+`   | `OUT1` || `M−`   | `OUT2` |电脑USB   ↓ESP32的5V/VIN   ↓L298N的5V逻辑端| 功能  | GPIO || --- | ---: || ENA |   13 || IN1 |   14 || IN2 |    4 || IN3 |   32 || IN4 |   23 || ENB |   33 || 编码器      |  ESP32 || -------- | -----: || 左编码器A    | GPIO18 || 左编码器B    | GPIO19 || 右编码器A    | GPIO25 || 右编码器B    | GPIO26 || 两个编码器VCC |    3V3 || 两个编码器GND |    GND |物理右编码器A → GPIO18物理右编码器B → GPIO19交换信号线物理左编码器A → GPIO25物理左编码器B → GPIO26| 系统         | 起点           | 终点               || ---------- | ------------ | ---------------- || 左PWM       | ESP32 GPIO13 | L298N ENA        || 左方向1       | ESP32 GPIO14 | L298N IN1        || 左方向2       | ESP32 GPIO4  | L298N IN2        || 右方向1       | ESP32 GPIO32 | L298N IN3        || 右方向2       | ESP32 GPIO23 | L298N IN4        || 右PWM       | ESP32 GPIO33 | L298N ENB        || 公共地        | ESP32 GND    | L298N GND        || L298N逻辑电源  | ESP32 5V/VIN | L298N 5V         || 左电机M+      | 左电机          | OUT1             || 左电机M−      | 左电机          | OUT2             || 右电机M+      | 右电机          | OUT3             || 右电机M−      | 右电机          | OUT4             || 左编码器V+     | 左编码器         | ESP32 3V3        || 左编码器GND    | 左编码器         | ESP32 GND        || 左编码器A      | 左编码器         | GPIO25           || 左编码器B      | 左编码器         | GPIO26           || 右编码器V+     | 右编码器         | ESP32 3V3        || 右编码器GND    | 右编码器         | ESP32 GND        || 右编码器A      | 右编码器         | GPIO18           || 右编码器B      | 右编码器         | GPIO19           || 电机电源正极     | 台式电源+        | L298N 12V/VIN/VS || 电机电源负极     | 台式电源−        | L298N GND        || 数据与ESP32供电 | 电脑USB        | ESP32 USB        || US-100引脚 | ESP32  || -------- | ------ || VCC      | 3.3V   || GND      | GND    || Trig/TX  | GPIO27 || Echo/RX  | GPIO34 |